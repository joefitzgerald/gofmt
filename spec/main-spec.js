'use babel'

describe('Main', () => {
  // let workspaceElement = null
  // let activationPromise = null

  beforeEach(() => {
    // workspaceElement = atom.views.getView(atom.workspace)
    // activationPromise = atom.packages.activatePackage('gofmt')
  })
})
